# GPS

Fresh workspace for the MPPI-GPS implementation planned in `mathforml`.

## Layout

- `docs/`: design notes, references, and legacy inventory
- `src/`: core implementation
- `scripts/`: runnable entry points and utilities
- `experiments/`: throwaway tests and prototypes

## Notes

- This folder lives inside the existing `mathforml` git repo.
- If you want a standalone GitHub repository later, the cleanest options are:
  - move `GPS/` out into its own top-level repo, or
  - keep it here for now and split it out later with git history tools
- Current legacy inventory: `docs/legacy_inventory.md`
