Put runnable demos, training entry points, and one-off utilities here.

Video helpers:
- `sync_videos.sh`: run locally to mirror remote `*.mp4` outputs over SSH with `rsync`
- `watch_latest.sh`: run locally to open the newest synced `*.mp4` whenever it changes
